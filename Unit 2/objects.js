"use strict"
//create new objects of the class from the library and then call new name after and log it
const newName = new newClass("john").newName();

//create new objects of the class from the library and then call new name after and log it
const newName1 = new newClass("Alfredo").newName();

//create new objects of the class from the library and then call new name after and log it
const newName2 = new newClass("Taylor").newName();




